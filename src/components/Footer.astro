---
import { Popover } from "@headlessui/react";

interface Props {
  className?: String
}
const { className="" } = Astro.props

const quote = "Lorem impsum dolor sit amet. Lorem impsum dolor sit amet."
---

<Popover className={`${className}`}>
  <div class="mx-auto max-w-7xl px-4 sm:px-6">
    <div class="flex items-center justify-between border-t-2 border-gray-100 py-6 text-xs" >
      <!-- Quote (left) -->
      <span class="self-start">{quote}</span>

      <!-- Links -->
      <ul class="flex flex-col xs:flex-row ml-4 gap-x-5">
        {["Imprint", "Data Protection"].map((o) => <li><a href={"/" + o.toString()} class="whitespace-nowrap text-gray-500 hover:text-gray-950 capitalize">{o}</a></li>)}
      </ul>
    </div>
  </div>
</Popover>
